$(document).ready(function() {
    if ($('.SignOut').length) {
        transientKey = $('#TransientKey').val();
        document.location = gdn.url('/entry/signout?TransientKey=' + transientKey);
    }
});
